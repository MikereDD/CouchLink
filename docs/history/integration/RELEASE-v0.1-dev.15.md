# CouchLink v0.1-dev.15

This integration build preserves the updated Windows launcher host 0.2-dev.1 and advances the Android client to 1.1-dev.1.

The Android app now discovers, pairs with, reconnects to, and sends launcher actions to the Windows host over the local network. Bluetooth HID remains the independent input path and automatic launcher fallback.
