# CouchLink v0.2-dev.7

Settings cleanup:

- Removed the obsolete Windows Host features panel.
- Removed the duplicate Windows Host connection panel.
- Removed obsolete Host fields from copied diagnostics.
- Kept Bluetooth HID setup, persistent connection, haptics, natural scrolling, About, Steam Big Picture, and the verified EA direct-launch fix unchanged.
