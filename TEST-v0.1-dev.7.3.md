# CouchLink v0.1-dev.7.3 Test

## Identity checks

1. Build and launch the Windows host.
2. Confirm the metallic orange/silver CL mark is visibly larger in the custom title bar.
3. Confirm the CL mark is clear in the Windows taskbar and Alt+Tab switcher.
4. Build and install/update the Android APK.
5. Confirm the CL mark is noticeably larger in the CouchLink header.
6. Confirm the package installer/update card and Android launcher show a readable CL mark rather than a tiny centered badge.
7. Check recent-apps/overview icon presentation.

If Android caches the previous launcher icon, uninstall CouchLink once and install the new APK fresh before judging launcher-icon scale.

## Regression checks

- Trusted reconnect and heartbeat
- Input lock synchronization
- Touchpad, scrolling, clicks, and drag lock
- Keyboard text and special keys
- Steam Big Picture and all secondary launchers
- Command Deck actions
- Windows title-bar drag, resize, minimize, maximize, and close
