<Window x:Class="CouchLink.Host.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="CouchLink Host"
        Icon="/Assets/couchlink.ico"
        Width="1180"
        Height="760"
        MinWidth="980"
        MinHeight="650"
        Background="{StaticResource SurfaceBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="50" ResizeBorderThickness="7" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="46" />
                    <ColumnDefinition Width="46" />
                    <ColumnDefinition Width="46" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                    <Image Source="/Assets/couchlink.png" Width="38" Height="38" Stretch="Uniform" />
                    <TextBlock Margin="8,0,0,0" Text="CouchLink Host" VerticalAlignment="Center" FontWeight="SemiBold" />
                </StackPanel>
                <Button Grid.Column="1" Content="—" Click="Minimize_Click" Background="Transparent" Foreground="{StaticResource TextBrush}" BorderThickness="0" FontSize="16" shell:WindowChrome.IsHitTestVisibleInChrome="True" />
                <Button Grid.Column="2" Content="□" Click="MaximizeRestore_Click" Background="Transparent" Foreground="{StaticResource TextBrush}" BorderThickness="0" FontSize="14" shell:WindowChrome.IsHitTestVisibleInChrome="True" />
                <Button Grid.Column="3" Content="×" Click="Close_Click" Background="Transparent" Foreground="{StaticResource TextBrush}" BorderThickness="0" FontSize="20" shell:WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="*" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock FontSize="30" FontWeight="SemiBold">
                    <Run Text="Couch"/><Run Text="Link" Foreground="{StaticResource AccentBrush}"/><Run Text=" Host"/>
                </TextBlock>
                <TextBlock Margin="0,8,0,0"
                           Text="Your Windows PC, from the couch."
                           FontSize="16"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
            <Border Grid.Column="1"
                    Padding="14,8"
                    VerticalAlignment="Center"
                    CornerRadius="14"
                    Background="{StaticResource PanelRaisedBrush}">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="9" Height="9" Margin="0,0,8,0" Fill="{StaticResource SuccessBrush}" />
                    <TextBlock Text="v0.1-dev.12.1 — Virtual HID Safety Review" FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.55*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Padding="24" Background="{StaticResource PanelBrush}" CornerRadius="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="22" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="22" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Host status" FontSize="20" FontWeight="SemiBold" />

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Development host" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding HostName}" FontSize="24" FontWeight="SemiBold" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Host state" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding HostState}" FontSize="18" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.15*" />
                            <ColumnDefinition Width="1.15*" />
                            <ColumnDefinition Width="0.7*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Boot Service" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding BootServiceStatus}" FontSize="16" Foreground="{StaticResource SuccessBrush}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding BootMachineState}" Foreground="{StaticResource AccentBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="18,0,0,0">
                            <TextBlock Text="Session Host" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding SessionHostStatus}" FontSize="16" Foreground="{StaticResource SuccessBrush}" TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ServiceStatus}" Foreground="{StaticResource MutedTextBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="18,0,0,0">
                            <TextBlock Text="Clients" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Margin="0,4,0,0" Text="{Binding ConnectedClients}" FontSize="22" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="6" Padding="18" Background="{StaticResource PanelRaisedBrush}" CornerRadius="14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Discovery" Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding DiscoveryStatus}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding DiscoveryEndpoint}" Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Session endpoint" Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Margin="0,5,0,0" Text="{Binding SessionEndpoint}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,5,0,0" Text="Pairing required" Foreground="{StaticResource AccentBrush}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Padding="24" Background="{StaticResource PanelBrush}" CornerRadius="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="Device and input control" FontSize="20" FontWeight="SemiBold" />
                        <TextBlock Margin="0,12,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}"
                                   Text="Pair new remotes, revoke old devices, and control remote mouse and keyboard access." />

                        <Border Margin="0,18,0,0" Padding="16" CornerRadius="12" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="PAIRING" Foreground="{StaticResource MutedTextBrush}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,8,0,0" Text="{Binding PairingCodeDisplay}" FontSize="30" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                                <TextBlock Margin="0,7,0,0" Text="{Binding PairingHelpText}" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" />
                                <Button Margin="0,12,0,0" Padding="14,10" Command="{Binding PairNewDeviceCommand}" Content="PAIR NEW DEVICE" />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,16,0,0" Padding="16" CornerRadius="12" Background="{StaticResource PanelRaisedBrush}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="TRUSTED DEVICES" Foreground="{StaticResource MutedTextBrush}" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="1" Text="{Binding TrustedDevices}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" />
                                </Grid>
                                <ListBox Margin="0,10,0,0" Height="120" ItemsSource="{Binding TrustedDeviceList}" SelectedItem="{Binding SelectedTrustedDevice}"
                                         Background="Transparent" BorderBrush="#343A43" Foreground="{StaticResource TextBrush}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="6">
                                                <TextBlock Text="{Binding ClientName}" FontWeight="SemiBold" />
                                                <TextBlock Margin="0,3,0,0" Text="{Binding ClientId}" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
                                                <TextBlock Margin="0,3,0,0" Text="{Binding PairedAtUtc, StringFormat={}Paired: {0:g}}" FontSize="11" Foreground="{StaticResource MutedTextBrush}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Button Padding="10,8" Command="{Binding RevokeSelectedDeviceCommand}" Content="REVOKE SELECTED" />
                                    <Button Grid.Column="2" Padding="10,8" Command="{Binding RevokeAllDevicesCommand}" Content="REVOKE ALL" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Margin="0,16,0,0" Padding="16" CornerRadius="12" Background="{StaticResource PanelRaisedBrush}">
                            <StackPanel>
                                <TextBlock Text="CONNECTED DEVICE" Foreground="{StaticResource MutedTextBrush}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,7,0,0" Text="{Binding ConnectedDevice}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,16,0,0" Padding="16" CornerRadius="12" Background="{StaticResource PanelRaisedBrush}">
                            <StackPanel>
                                <TextBlock Text="REMOTE INPUT" Foreground="{StaticResource MutedTextBrush}" FontWeight="SemiBold" />
                                <TextBlock Margin="0,7,0,0" Text="{Binding RemoteInputStatus}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                                <Button Margin="0,12,0,0" Padding="14,10" Command="{Binding ToggleRemoteInputCommand}" Content="{Binding RemoteInputButtonText}" />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,16,0,0" Padding="16" CornerRadius="12" Background="{StaticResource PanelRaisedBrush}">
                            <StackPanel>
                                <TextBlock Text="STARTUP" Foreground="{StaticResource MutedTextBrush}" FontWeight="SemiBold" />
                                <CheckBox Margin="0,12,0,0" Content="Start CouchLink with Windows" IsChecked="{Binding StartWithWindows}" />
                                <CheckBox Margin="0,10,0,0" Content="Start minimized to tray" IsChecked="{Binding StartMinimized}" />
                                <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" Text="Wake-on-LAN details are advertised securely to paired remotes on the local network." />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,16,0,0" Padding="16" CornerRadius="12" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Last host event" Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Margin="0,7,0,0" Text="{Binding LastEvent}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{StaticResource SuccessBrush}" />
                <TextBlock Text="Session Host active" Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>
            <TextBlock Grid.Column="1" Foreground="{StaticResource MutedTextBrush}">
                <Run Text="Host ID: " />
                <Run Text="{Binding HostId, Mode=OneWay}" Foreground="{StaticResource TextBrush}" />
            </TextBlock>
        </Grid>
        </Grid>
    </Grid>
</Window>
