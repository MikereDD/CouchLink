# CouchLink v0.1-dev.8.1.1 Test Checklist

1. Run `dotnet build` from `src\windows`.
2. Confirm the build succeeds with no CA1416 registry-platform warnings.
3. Launch CouchLink Host and confirm the badge reads `v0.1-dev.8.1.1 — Build Hotfix`.
4. Build and install the Android APK.
5. Confirm Home and Keyboard content scroll fully above bottom navigation.
6. Re-test launcher, touchpad, keyboard, pairing, and reconnect behavior.
